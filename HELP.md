# 本地知识图谱整合器 · 帮助

把文档与联网信息整理成可导航的 3D 知识图谱。全流程本地运行，图谱数据、图片、API Key 全部保存在本机数据目录（npm 安装模式在 `~/.local-knowledge-graph/`，Git 克隆模式在项目内 `data/`），代码仓库不包含任何用户数据。

## 查看当前版本

- 网页端：页头「**视图设置**」面板底部，或页头「**帮助**」面板的"版本与更新"区域，显示当前版本号
- 命令行：`node -e "console.log(require('./package.json').version)"`

## 检查更新 / 一键更新

1. 打开页头「**帮助**」面板（或「视图设置」面板底部）的"版本与更新"区域
2. 点击「**检查更新**」—— 应用会连接 GitHub 对比本地与远端版本，并显示最新发布说明
3. 若有新版本，点击「**一键更新**」—— 应用自动下载更新并重启服务，页面随后自动刷新

注意事项：

- 一键更新要求应用目录是本仓库的 Git 克隆（默认安装方式即是），且本地没有未发布的代码改动
- 更新只替换程序代码；`data/` 目录（图谱数据、图片、配置）完全不受影响
- 若自动重启失败，关闭窗口后重新运行启动脚本即可（`start.bat` / `start.command` / `bash start.sh`）

## 手动更新（备用方式）

npm 安装模式：

```bash
npm i -g local-knowledge-graph@latest
```

Git 克隆模式：

```bash
cd 项目目录
git pull --ff-only origin master
```

然后重新启动应用。

## 启动方式

**npm 安装（推荐）**：

```bash
npm i -g local-knowledge-graph
kg
```

数据保存在用户目录 `~/.local-knowledge-graph/`（Windows 在 `C:\Users\你\.local-knowledge-graph`），升级与卸载程序均不影响数据。也可 `npx local-knowledge-graph` 免安装试用。

**脚本启动（Git 克隆模式）**：

| 平台 | 方式 |
| ---- | ---- |
| Windows | 双击 `start.bat` |
| macOS | 双击 `start.command` |
| Linux / 命令行 | `bash start.sh` 或 `npm start` |

此模式数据存放在项目内 `data/` 目录。启动后浏览器访问 `http://localhost:3000`。依赖 Node.js >= 22.5；自然语言补全需要全局安装 `opencode` CLI（`npm i -g opencode-ai`），未安装时仅该功能降级，其余功能正常。

## API Key 配置（全部在界面上完成）

程序自身零内置密钥，所有需要 key 的能力都在界面上自由配置：

- **语义检索 / 智能提问综述**：检索页签"语义检索配置"填入 API Key（默认硅基流动 BAAI/bge-m3，可换任意 OpenAI 兼容接口的 Base URL 与模型）。Key 仅存本地数据目录的 settings.json，服务端永不回传真实值。不配置则自动退化为关键词检索，其余功能不受影响。
- **OpenCode（AI 对话建图 / 智能提问编译）**：使用 opencode CLI 自身的认证（`opencode auth login`），应用不经手密钥。未安装或未认证时仅这两个功能降级。
- 更换数据目录（`kg --data <目录>`）会使用该目录下的独立配置，互不串扰。

## 功能速览

- **AI 对话建图**：右侧输入自然语言指令（联网补全），或上传文档（md/txt/pdf/docx）批量抽取三元组
- **智能提问**：检索页签一句话找关系（LLM 编译检索计划，本地只读执行，可生成带引用的综述）
- **语义检索**：配置硅基流动 API Key 后支持语义+关键词混合检索（未配置时自动退化为关键词模式）
- **最短路径 / 中心子图 / 推理**：挖掘实体间关联链路、层级范围与隐性关系
- **撤销**：Ctrl+Z 或按钮，可连续撤销
- **浅色主题**：视图设置 → 界面主题 → 浅色，整站配色与 3D 画布同步切换，选择本地记忆
- **保存点与回溯**：版本页签打保存点，随时回滚；每次保存点自动滚动备份到 `data/backups/`
- **图片绑定**：实体信息卡上传本地图片
- **导出**：RDF(Turtle)、整库 .db、单文件只读网页版

## 键盘快捷键

| 快捷键 | 功能 |
| ------ | ---- |
| Ctrl+F | 聚焦搜索框 |
| Ctrl+Z | 撤销最近操作 |
| Delete | 删除选中实体/关系 |
| Esc | 逐层关闭面板 / 取消选中 |

## 常见问题

**Q：打开页面是空白？**
用现代浏览器（Chrome/Edge/Safari 最新版）；确认服务已启动且端口未被占用。

**Q：OpenCode 检测未就绪？**
运行 `npm i -g opencode-ai` 安装后重启应用；OpenCode 仅用于自然语言补全与智能提问的编译环节。

**Q：数据会上传到云端吗？**
图谱数据只存在本机。仅当你在"检索"页签配置了语义检索 API Key 时，检索向量化的文本片段才会发往你配置的模型服务；OpenCode 补全公开知识时按其自身机制联网。其余全部本地完成。

**Q：GitHub 仓库和 npm 包版本如何保持一致？**
仓库内置 GitHub Actions（`.github/workflows/publish.yml`）：只要推送 `v*` 格式的标签，流水线会自动校验"标签版本 = package.json 版本"→ 跑全部测试 → 发布 npm → 创建 GitHub Release。即：改 package.json 版本 → 提交 → 打同名 tag → push，两端自动同步，人工只需打一次标签。

**Q：如何回滚某次误操作？**
优先用「撤销」（可连续）；需要回到更早状态时在"版本"页签回溯到对应保存点。

## 版本历史摘要

- **v1.6.0**：界面主题新增浅色配色（视图设置一键切换，本地记忆）；GitHub 仓库与 npm 包通过 Actions 自动同步（打 tag 即测试+发布+Release）

- **v1.5.0**：npm 包化（`npm i -g local-knowledge-graph` + `kg` 一键安装启动）；数据目录迁至用户目录（升级无损）；应用内更新支持 npm 与 git 双模式

- **v1.4.0**：设置与帮助面板内置版本号、检查更新、一键更新；项目目录新增本帮助文档
- **v1.3.0**：智能关系检索（自然语言提问 → 检索计划 → 关联发现 → 综述）
- **v1.2.0**：连续撤销、最短路径查询、日志筛选、悬浮提示、键盘快捷键
- **v1.1.0**：向量库拆分、导入去重加固、AI 任务互斥、聚焦飞行、日志人话渲染、推理持久化、单文件网页导出、移动端适配、一键启动脚本
- **v1.0.0**：首版——三表数据模型、3D 可视化、RDF 校验、Git 保存点/回溯、文档导入、OWL 推理、MCP Server

完整发布说明见 [GitHub Releases](https://github.com/iamsamyiok/local-knowledge-graph/releases)。
